import type { BuildRecommendation } from "@/types";

// Collection: builds — per-game, per-character/killer, current recommended
// builds + metadata (spec section 6)
export type BuildDoc = BuildRecommendation;
